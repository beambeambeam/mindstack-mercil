{
  "name": "api",
  "version": "1.0.0",
  "scripts": {
    "dev": "uv run python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "start": "uv run python -m uvicorn main:app --host 0.0.0.0 --port 8000",
    "setup": "uv venv",
    "sync": "uv sync"
  }
}
