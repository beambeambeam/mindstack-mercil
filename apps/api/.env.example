# CORS Configuration
# Use "*" to allow all origins, or comma-separated list: "http://localhost:3000,https://example.com"
CORS_ORIGINS=*

# Server Configuration
# Use "localhost" for local-only access, or "0.0.0.0" to allow network access
HOST=localhost
PORT=3001

#DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
