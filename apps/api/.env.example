# CORS Configuration
# Use "*" to allow all origins, or comma-separated list: "http://localhost:3000,https://example.com"
CORS_ORIGINS=*

# Server Configuration
# Use "localhost" for local-only access, or "0.0.0.0" to allow network access
HOST=localhost
PORT=3001

#DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Database Configuration (for local API connection)
DATABASE_URL=postgresql://postgres:changeme@localhost:5432/mindstack_mercil_db
POSTGRES_PASSWORD=changeme

# Ollama Configuration (for local API connection)
OLLAMA_BASE_URL=http://localhost:11434

# pgAdmin Configuration
PGADMIN_EMAIL=admin@mercil.com
PGADMIN_PASSWORD=admin
